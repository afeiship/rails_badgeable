{
  "name": "rails_badgeable",
  "version": "1.0.1",
  "private": true,
  "description": "Add limited, reusable badges to multiple models via polymorphic associations.",
  "scripts": {
    "dev": "cd test/dummy && rails server",
    "clean": "rm -rf *.gem",
    "build": "gem build *.gemspec",
    "pubpush": "gem push *.gem",
    "release": "release-it"
  },
  "author": "afeiship",
  "keywords": [
    "rails",
    "badgeable"
  ]
}
